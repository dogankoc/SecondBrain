# Second Brain by Pijkard

This vault is {{USER_NAME}}'s persistent AI memory layer.
Preferred language: {{LANGUAGE}}.

Read `Memory/Core.md`, `Memory/Last-Session.md`, `Memory/Threads.md`, `Memory/Rules.md`, `knowledge/index.md`, and `index.md` as lightweight routing context when relevant.

Project-local files remain authoritative for project-specific facts. `raw/` is immutable. Preserve provenance, surface contradictions, avoid duplicate canonical pages, archive instead of deleting, and never store secrets.
