# Second Brain

This vault is {{USER_NAME}}'s persistent AI memory layer.
Preferred language: {{LANGUAGE}}.

Use `Memory/`, `knowledge/index.md`, and `index.md` as routing context when relevant. Project-local instructions remain authoritative for project-specific facts.

Hard rules: `raw/` is immutable; preserve provenance; surface contradictions; avoid duplicates; archive instead of deleting; never store secrets.

---

_Authored and maintained by [Doğan Koç](https://github.com/dogankoc)._
